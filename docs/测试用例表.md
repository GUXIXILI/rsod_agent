# 测试用例表

> 编写日期：2026-07-14  
> 总用例数：35 个（P0: 18，P1: 14，P2: 3）

## 1. 主调度智能体路由测试

| 编号 | 测试场景 | 前置条件 | 输入 | 预期输出 | 优先级 |
|------|---------|---------|------|---------|--------|
| TC‑AGENT‑001 | 路由到检测智能体 | 用户已登录，存在活跃会话 | `POST /api/chat/messages`，消息内容："帮我检测这张图片中的火灾隐患" | 响应中 `agent_route` 字段为 `detection`，调用检测服务 | P0 |
| TC‑AGENT‑002 | 路由到分析智能体 | 用户已登录，存在活跃会话 | `POST /api/chat/messages`，消息内容："分析一下最近一周的火情趋势" | 响应中 `agent_route` 字段为 `analysis`，调用统计服务 | P0 |
| TC‑AGENT‑003 | 路由到问答智能体 | 用户已登录，存在活跃会话 | `POST /api/chat/messages`，消息内容："什么是火灾预警等级？" | 响应中 `agent_route` 字段为 `qa`，返回知识库答案 | P0 |
| TC‑AGENT‑004 | 模糊意图默认路由 | 用户已登录，存在活跃会话 | `POST /api/chat/messages`，消息内容："你好" | 路由到默认智能体（问答），返回友好回复 | P1 |
| TC‑AGENT‑005 | 路由上下文连续性 | 用户已登录，会话中有历史消息 | 连续发送多条消息，第二条为"继续刚才的分析" | 正确继承上一轮智能体上下文 | P1 |

## 2. 检测智能体工具调用测试

| 编号 | 测试场景 | 前置条件 | 输入 | 预期输出 | 优先级 |
|------|---------|---------|------|---------|--------|
| TC‑TOOL‑001 | 调用单图检测工具 | 用户已登录，图片已上传至 MinIO | 调用检测工具，传入图片路径 | 返回检测结果（bbox、置信度、类别） | P0 |
| TC‑TOOL‑002 | 调用批量检测工具 | 用户已登录，多张图片已上传 | 调用检测工具，传入图片路径列表（≤20张） | 返回每张图片的检测结果列表 | P0 |
| TC‑TOOL‑003 | 调用视频检测工具 | 用户已登录，视频已上传 | 调用检测工具，传入视频路径 | 返回逐帧检测结果 | P1 |
| TC‑TOOL‑004 | 工具调用异常——图片不存在 | 用户已登录 | 调用检测工具，传入不存在的图片路径 | 返回明确错误信息，不崩溃 | P1 |
| TC‑TOOL‑005 | 工具调用超时处理 | 用户已登录 | 调用检测工具，模拟推理超时 | 超时后返回超时提示，任务状态标记为失败 | P2 |
| TC‑TOOL‑006 | 工具调用结果格式化 | 用户已登录 | 检测工具返回原始结果 | 结果按统一 Schema 格式化，包含 `tool_name`、`status`、`data`、`error`（如有） | P1 |

## 3. 压缩包检测功能测试

| 编号 | 测试场景 | 前置条件 | 输入 | 预期输出 | 优先级 |
|------|---------|---------|------|---------|--------|
| TC‑ZIP‑001 | 上传并检测 ZIP 压缩包 | 用户已登录，ZIP 包含多张图片 | `POST /api/detection/zip`，上传 ZIP 文件 | 解压后逐张检测，返回汇总结果 | P0 |
| TC‑ZIP‑002 | 压缩包内图片格式支持 | 用户已登录，ZIP 包含 jpg/png/webp 混合格式 | 上传混合格式 ZIP | 所有支持格式图片均被检测 | P0 |
| TC‑ZIP‑003 | 压缩包内包含非图片文件 | 用户已登录，ZIP 包含图片 + txt 文件 | 上传含非图片文件的 ZIP | 仅检测图片文件，忽略非图片，返回说明 | P1 |
| TC‑ZIP‑004 | 空压缩包上传 | 用户已登录 | 上传空 ZIP 文件 | 返回错误提示："压缩包为空" | P1 |
| TC‑ZIP‑005 | 超大压缩包上传 | 用户已登录 | 上传超过大小限制的 ZIP（如 >500MB） | 返回错误提示："文件大小超出限制" | P1 |
| TC‑ZIP‑006 | 压缩包解压失败 | 用户已登录 | 上传损坏的 ZIP 文件 | 返回错误提示："压缩包解压失败" | P2 |
| TC‑ZIP‑007 | 批量检测结果汇总 | 用户已登录，ZIP 含多张图片 | 上传 ZIP 完成检测 | 返回汇总统计：总图片数、检出火灾图片数、各等级分布 | P1 |

## 4. 对话页面组件测试（前端 UI）

| 编号 | 测试场景 | 前置条件 | 输入/操作 | 预期输出 | 优先级 |
|------|---------|---------|----------|---------|--------|
| TC‑UI‑001 | 消息输入框渲染 | 页面加载完成 | 查看页面 | 输入框可见，支持文本输入 | P0 |
| TC‑UI‑002 | 发送消息（非流式） | 用户已登录 | 输入消息，点击发送 | 消息显示在对话区，AI 回复出现 | P0 |
| TC‑UI‑003 | 发送消息（SSE 流式） | 用户已登录 | 输入消息，点击发送 | AI 回复逐字（逐 token）显示 | P0 |
| TC‑UI‑004 | 会话列表展示 | 用户已登录，存在历史会话 | 进入对话页面 | 左侧显示会话列表，按时间排序 | P0 |
| TC‑UI‑005 | 创建新会话 | 用户已登录 | 点击"新建会话"按钮 | 新会话创建成功，清空当前对话区 | P0 |
| TC‑UI‑006 | 切换会话 | 用户已登录，存在多个会话 | 点击不同会话 | 加载对应会话的历史消息 | P0 |
| TC‑UI‑007 | 删除会话 | 用户已登录，存在会话 | 点击会话的删除按钮 | 会话被删除，消息级联删除 | P1 |
| TC‑UI‑008 | 消息加载状态 | 用户已登录 | 发送消息后 | 显示加载中动画/占位符 | P1 |
| TC‑UI‑009 | 消息渲染——图片/表格 | 用户已登录 | AI 回复包含图片或表格 | 图片/表格正确渲染 | P1 |
| TC‑UI‑010 | 响应式布局 | - | 调整窗口大小 | 对话区自适应 | P2 |

## 5. 前后端联调集成测试

| 编号 | 测试场景 | 前置条件 | 测试步骤 | 预期输出 | 优先级 |
|------|---------|---------|---------|---------|--------|
| TC‑E2E‑001 | 完整对话-检测链路 | 用户已登录，测试图片已准备 | ① 输入"帮我检测这张图片" → ② 上传图片 → ③ 发送消息 | AI 识别意图，调用检测服务，返回检测结果 | P0 |
| TC‑E2E‑002 | 完整对话-分析链路 | 用户已登录，历史数据存在 | ① 输入"分析最近火情趋势" → ② 发送 | AI 识别意图，调用统计服务，返回趋势分析 | P0 |
| TC‑E2E‑003 | 多轮对话上下文保持 | 用户已登录 | ① "列出所有检测任务" → ② "第一个的详情" | 第二轮正确引用第一轮结果中的任务 ID | P0 |
| TC‑E2E‑004 | 认证拦截 | 未登录 | 直接访问 `/api/chat/messages` | 返回 401 Unauthorized | P0 |
| TC‑E2E‑005 | SSE 流式完整链路 | 用户已登录 | 发送消息到 `/api/chat/messages/stream` | 事件流正常，逐 token 返回，最终以 `[DONE]` 结束 | P0 |
| TC‑E2E‑006 | 错误恢复——后端异常 | 用户已登录 | 模拟后端服务异常 | 前端显示友好错误提示，不白屏 | P1 |
| TC‑E2E‑007 | 超时处理 | 用户已登录 | 模拟检测任务超时（>30s） | 前端显示超时提示，允许重试 | P1 |

---

**总结**：共 **35** 个测试用例，其中 P0 级 18 个，P1 级 14 个，P2 级 3 个。
